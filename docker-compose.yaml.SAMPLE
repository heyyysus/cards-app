services:
  web:
    build: ./service
    ports: 
      - "5000:5000"
    volumes:
      - ./service/src:/app/src
    depends_on:
      - db
    environment:
      - APPLICATION_PORT=
      - POSTGRES_HOST=db
      - POSTGRES_PORT=
      - POSTGRES_DB=
      - POSTGRES_USER=
      - POSTGRES_PASSWORD=
      - OAUTH_CLIENT_DOMAIN=
      - OAUTH_CLIENT_ID=
      - OAUTH_CLIENT_SECRET=
  db:
    image: postgres:15.2
    restart: always
    environment:
      - POSTGRES_USER=
      - POSTGRES_PASSWORD=
    ports:
      - '5434:5432'

volumes:
  db:
    driver: local
